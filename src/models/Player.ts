/**
 * Represents a person playing a Game.
 */
export interface Player {
    /**
     * ID of the player.
     */
    id: string;

    /**
     * Name of the player.
     */
    name: string;

    /**
     * Role of the player.
     */
    role: string;
}